export default class card{
    constructor(title, skill, cat, desc, hLink){
        this.title = title;
        this.skill = skill;
        this.cat = cat;
        this.desc = desc;
        this.hLink = hLink;
    }

    makecard(){
        ///TODO
            //fazer a logica do HTML aqui
    }
}